# ⚡ راهنمای سریع: فعال کردن GitHub Pages

## 🎯 مراحل (2 دقیقه)

### 1. فعال کردن GitHub Pages

1. به Repository خود بروید:
   ```
   https://github.com/ZahraAzadeh/Float_Translation_Button
   ```

2. روی **Settings** کلیک کنید (در بالای صفحه، کنار Code)

3. در منوی سمت چپ، **Pages** را پیدا کنید و کلیک کنید

4. در بخش **Build and deployment**:
   - **Source**: `Deploy from a branch` را انتخاب کنید
   - **Branch**: `main` را انتخاب کنید
   - **Folder**: `/ (root)` را انتخاب کنید
   - روی **Save** کلیک کنید

5. چند دقیقه صبر کنید (معمولاً 1-2 دقیقه)

6. آدرس برنامه شما:
   ```
   https://zahraazadeh.github.io/Float_Translation_Button/
   ```

### 2. تست کردن

1. آدرس بالا را در مرورگر باز کنید
2. باید برنامه شما نمایش داده شود ✅

### 3. استفاده روی موبایل

1. آدرس را در مرورگر موبایل باز کنید
2. منو > "Add to Home Screen" (Android)
3. یا Share > "Add to Home Screen" (iOS)

---

## ✅ بررسی کارکرد

بعد از فعال کردن، این موارد باید کار کنند:
- ✅ صفحه اصلی نمایش داده می‌شود
- ✅ دکمه شناور نمایش داده می‌شود
- ✅ Service Worker ثبت می‌شود
- ✅ می‌توانید به صورت PWA نصب کنید

---

## 🔧 اگر کار نکرد

### مشکل: 404 Error

**راه‌حل**:
- مطمئن شوید که `index.html` در root directory است
- چند دقیقه دیگر صبر کنید (GitHub Pages نیاز به زمان دارد)

### مشکل: Service Worker کار نمی‌کند

**راه‌حل**:
- مطمئن شوید که `sw.js` در root directory است
- در Console مرورگر بررسی کنید (F12)

### مشکل: فایل‌ها لود نمی‌شوند

**راه‌حل**:
- مطمئن شوید که تمام فایل‌ها Push شده‌اند
- مسیر فایل‌ها را بررسی کنید

---

## 📱 لینک نهایی

بعد از فعال کردن GitHub Pages، آدرس شما:
```
https://zahraazadeh.github.io/Float_Translation_Button/
```

این آدرس را در موبایل باز کنید و نصب کنید! 🎉

