# تحلیل نیازمندی‌های اپلیکیشن ترجمه شناور

## 📋 تحلیل کلی برنامه

### هدف اصلی
ایجاد یک اپلیکیشن ترجمه شناور که روی شبکه‌های اجتماعی و سایر اپلیکیشن‌ها به صورت Overlay نمایش داده شود و امکان ترجمه سریع متن را فراهم کند.

---

## 🎯 نیازمندی‌های کاربر

### 1. رابط کاربری (UI/UX)
- **دکمه شناور (Floating Button)**: 
  - همیشه در دسترس باشد
  - قابل جابجایی در صفحه
  - طراحی ساده و غیر مزاحم
  
- **کادر ورودی متن (Input Box)**:
  - امکان Paste کردن متن
  - نمایش پرچم زبان منبع
  - رابط کاربری ساده و واضح
  
- **کادر خروجی ترجمه (Output Box)**:
  - نمایش متن ترجمه شده
  - نمایش پرچم زبان هدف
  - امکان کپی کردن متن ترجمه شده
  
- **دکمه‌های کنترلی**:
  - دکمه بستن (Close)
  - دکمه تغییر زبان (Swap Languages)
  - دکمه بازنشانی (Reset/Undo)

### 2. قابلیت‌های اصلی

#### تشخیص خودکار زبان (Auto Language Detection)
- تشخیص خودکار زبان متن ورودی
- نمایش زبان تشخیص داده شده
- امکان تغییر دستی زبان در صورت نیاز

#### ترجمه متن
- ترجمه از زبان منبع به زبان هدف
- پشتیبانی از زبان‌های متعدد
- ترجمه سریع و دقیق

#### مدیریت زبان‌ها
- انتخاب زبان منبع
- انتخاب زبان هدف
- امکان جابجایی زبان‌ها (Swap)
- ذخیره تنظیمات زبان‌های اخیر

### 3. قابلیت‌های اضافی

#### تجربه کاربری
- انیمیشن‌های نرم برای باز/بسته شدن
- طراحی Responsive
- پشتیبانی از تم تاریک/روشن
- نمایش وضعیت ترجمه (Loading, Success, Error)

#### عملکرد
- ترجمه سریع (Real-time یا با دکمه)
- ذخیره تاریخچه ترجمه‌ها
- امکان جستجو در تاریخچه
- بهینه‌سازی برای استفاده کم از منابع

#### دسترسی‌پذیری
- پشتیبانی از صفحه‌خوان‌ها (Screen Readers)
- امکان تغییر اندازه فونت
- پشتیبانی از RTL (راست به چپ) برای زبان‌های فارسی و عربی

### 4. نیازمندی‌های فنی

#### API ترجمه
- استفاده از Google Translate API یا Microsoft Translator
- یا استفاده از کتابخانه‌های Open Source مثل LibreTranslate
- مدیریت خطاها و Retry Logic

#### ذخیره‌سازی
- ذخیره تنظیمات کاربر
- ذخیره تاریخچه ترجمه‌ها (اختیاری)
- Cache برای ترجمه‌های تکراری

#### امنیت
- عدم ارسال اطلاعات حساس به سرور
- رمزگذاری ارتباطات
- رعایت حریم خصوصی کاربر

---

## 🏗️ معماری پیشنهادی

### ساختار فایل‌ها
```
floating-translation-overlay/
├── index.html          # صفحه اصلی
├── styles/
│   ├── main.css        # استایل‌های اصلی
│   └── themes.css      # تم‌های تاریک/روشن
├── scripts/
│   ├── app.js          # منطق اصلی برنامه
│   ├── translation.js  # مدیریت ترجمه
│   ├── language-detection.js  # تشخیص زبان
│   └── ui.js           # مدیریت رابط کاربری
├── assets/
│   └── flags/          # تصاویر پرچم‌ها
└── config.js           # تنظیمات و API Keys
```

### تکنولوژی‌های پیشنهادی
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla یا React)
- **Translation API**: Google Translate API / LibreTranslate
- **Language Detection**: franc.js یا Google Language Detection
- **Storage**: LocalStorage برای تنظیمات

---

## 📱 جریان کار کاربر (User Flow)

1. کاربر روی دکمه شناور کلیک می‌کند
2. کادر ترجمه باز می‌شود
3. کاربر متن را Paste می‌کند یا تایپ می‌کند
4. سیستم به صورت خودکار زبان را تشخیص می‌دهد
5. متن به زبان هدف ترجمه می‌شود
6. کاربر می‌تواند:
   - متن ترجمه شده را کپی کند
   - زبان‌ها را جابجا کند
   - زبان جدید انتخاب کند
   - کادر را ببندد

---

## ✅ چک‌لیست ویژگی‌ها

- [x] دکمه شناور
- [x] کادر ورودی با پرچم زبان
- [x] کادر خروجی با پرچم زبان
- [x] تشخیص خودکار زبان
- [x] ترجمه متن
- [x] جابجایی زبان‌ها
- [x] دکمه بستن
- [x] طراحی Responsive
- [x] پشتیبانی از تم تاریک
- [x] انیمیشن‌های نرم
- [ ] تاریخچه ترجمه (اختیاری)
- [ ] ذخیره تنظیمات (اختیاری)

---

## 🎨 طراحی UI (بر اساس تصویر)

### رنگ‌بندی
- **پس‌زمینه تاریک**: #1e1e1e یا #2b2b2e
- **کادر ورودی**: آبی روشن (#4A90E2)
- **کادر خروجی**: قرمز/نارنجی (#FF6B6B)
- **دکمه‌ها**: آبی (#007AFF)
- **متن**: سفید/خاکستری روشن

### المان‌های UI
- آیکون ترجمه (دو فلش متقاطع)
- آیکون بازنشانی (فلش دایره‌ای)
- آیکون بستن (X در دایره قرمز)
- پرچم‌های کشورها
- کادرهای گفتگو (Speech Bubbles)

