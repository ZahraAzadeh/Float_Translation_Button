# 📤 راهنمای Push کردن تغییرات به GitHub

## 🎯 مراحل Push کردن تغییرات

### روش 1: استفاده از Command Line (Terminal)

#### مرحله 1: بررسی وضعیت

```bash
git status
```

این دستور فایل‌های تغییر یافته را نشان می‌دهد.

#### مرحله 2: اضافه کردن فایل‌ها

```bash
# اضافه کردن همه فایل‌های تغییر یافته
git add .

# یا اضافه کردن فایل‌های خاص
git add index.html scripts/app.js scripts/ui.js styles/main.css
```

#### مرحله 3: Commit کردن

```bash
git commit -m "رفع مشکلات: نمایش زبان در کادر منبع و افزودن دکمه انتخاب زبان"
```

یا با پیام انگلیسی:
```bash
git commit -m "Fix: Display detected language in source box and add language selection buttons"
```

#### مرحله 4: Push کردن

```bash
git push origin main
```

یا اگر branch شما `master` است:
```bash
git push origin master
```

---

### روش 2: استفاده از GitHub Desktop

1. **باز کردن GitHub Desktop**
2. **بررسی تغییرات**: در سمت چپ، فایل‌های تغییر یافته را می‌بینید
3. **Commit**: 
   - در پایین، یک پیام commit بنویسید
   - مثلاً: "رفع مشکلات: نمایش زبان در کادر منبع و افزودن دکمه انتخاب زبان"
   - روی **"Commit to main"** کلیک کنید
4. **Push**: روی **"Push origin"** کلیک کنید

---

### روش 3: استفاده از VS Code

1. **باز کردن VS Code** در پوشه پروژه
2. **Source Control** (Ctrl+Shift+G)
3. **Stage All Changes** (+ کنار Changes)
4. **Commit Message** بنویسید
5. **Commit** را بزنید
6. **Push** را بزنید (فلش بالا)

---

## 📋 فایل‌های تغییر یافته

فایل‌هایی که باید Push کنید:

- ✅ `index.html` - اضافه شدن دکمه‌های انتخاب زبان
- ✅ `scripts/app.js` - اصلاح تشخیص زبان و افزودن توابع setSourceLanguage/setTargetLanguage
- ✅ `scripts/ui.js` - افزودن منوی انتخاب زبان
- ✅ `styles/main.css` - استایل‌های جدید برای دکمه‌ها و منو

---

## ⚠️ نکات مهم

### قبل از Push:

1. **بررسی کنید که همه تغییرات درست هستند**
2. **تست کنید**: برنامه را در مرورگر تست کنید
3. **Commit Message واضح بنویسید**

### بعد از Push:

1. **بررسی GitHub**: به Repository خود بروید و مطمئن شوید که تغییرات Push شده‌اند
2. **تست GitHub Pages**: اگر GitHub Pages فعال است، چند دقیقه صبر کنید و سپس تست کنید

---

## 🔄 اگر Repository وجود ندارد

اگر این پوشه هنوز یک Git Repository نیست:

### مرحله 1: Initialize Git

```bash
git init
```

### مرحله 2: اضافه کردن Remote

```bash
git remote add origin https://github.com/ZahraAzadeh/Float_Translation_Button.git
```

### مرحله 3: اضافه کردن فایل‌ها

```bash
git add .
```

### مرحله 4: Commit اولیه

```bash
git commit -m "Initial commit: Floating Translation App"
```

### مرحله 5: Push

```bash
git branch -M main
git push -u origin main
```

---

## ✅ بررسی موفقیت

بعد از Push:

1. به Repository بروید: [https://github.com/ZahraAzadeh/Float_Translation_Button](https://github.com/ZahraAzadeh/Float_Translation_Button)
2. بررسی کنید که تغییرات جدید نمایش داده می‌شوند
3. اگر GitHub Pages فعال است، چند دقیقه صبر کنید
4. برنامه را در مرورگر تست کنید

---

## 🎉 تمام!

بعد از Push موفق، تغییرات شما در GitHub خواهد بود و می‌توانید از آن استفاده کنید!

**موفق باشید! 🚀**

