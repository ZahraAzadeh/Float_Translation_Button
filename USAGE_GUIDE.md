# راهنمای استفاده از اپلیکیشن ترجمه شناور

## 🚀 شروع سریع

### مرحله 1: باز کردن برنامه
فایل `index.html` را در مرورگر خود باز کنید.

### مرحله 2: استفاده از دکمه شناور
- دکمه شناور در گوشه پایین راست صفحه نمایش داده می‌شود
- روی آن کلیک کنید تا پنل ترجمه باز شود

### مرحله 3: وارد کردن متن
- متن خود را در کادر آبی رنگ (کادر ورودی) Paste کنید یا تایپ کنید
- زبان به صورت خودکار تشخیص داده می‌شود

### مرحله 4: ترجمه
- روی دکمه "ترجمه" کلیک کنید
- یا از میانبر `Ctrl + Enter` استفاده کنید
- متن ترجمه شده در کادر قرمز رنگ (کادر خروجی) نمایش داده می‌شود

### مرحله 5: کپی کردن
- روی آیکون کپی در کنار زبان هدف کلیک کنید
- متن ترجمه شده در کلیپ‌بورد کپی می‌شود

## 🔧 تنظیمات API

### استفاده از LibreTranslate (رایگان)

LibreTranslate یک سرویس ترجمه رایگان و Open Source است. برای استفاده:

1. فایل `scripts/config.js` را باز کنید
2. مطمئن شوید که `useLibreTranslate: true` است
3. URL سرور LibreTranslate را تنظیم کنید

**نکته**: اگر با خطای CORS مواجه شدید، باید از یک سرور LibreTranslate محلی استفاده کنید یا از API های دیگر استفاده کنید.

### استفاده از Google Translate API

1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. یک پروژه جدید ایجاد کنید
3. Google Translate API را فعال کنید
4. یک API Key ایجاد کنید
5. در فایل `scripts/config.js`:
   ```javascript
   useGoogleTranslate: true,
   googleTranslateApiKey: 'YOUR_API_KEY_HERE'
   ```

### استفاده از Microsoft Translator

1. به [Azure Portal](https://portal.azure.com/) بروید
2. یک سرویس Translator ایجاد کنید
3. API Key و Region را دریافت کنید
4. در فایل `scripts/config.js`:
   ```javascript
   useMicrosoftTranslate: true,
   microsoftTranslateKey: 'YOUR_API_KEY_HERE',
   microsoftTranslateRegion: 'YOUR_REGION'
   ```

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها

در فایل `styles/main.css`:

```css
.input-box {
    background: linear-gradient(135deg, #YOUR_COLOR_1 0%, #YOUR_COLOR_2 100%);
}

.output-box {
    background: linear-gradient(135deg, #YOUR_COLOR_3 0%, #YOUR_COLOR_4 100%);
}
```

### تغییر موقعیت دکمه شناور

```css
.floating-btn {
    bottom: 30px;  /* فاصله از پایین */
    right: 30px;   /* فاصله از راست */
}
```

### افزودن زبان جدید

در فایل `scripts/config.js`:

```javascript
languages: {
    'new_lang': { 
        name: 'نام زبان', 
        flag: '🏳️', 
        code: 'new_lang' 
    }
}
```

## 🔍 ویژگی‌های پیشرفته

### ترجمه خودکار

برای فعال کردن ترجمه خودکار هنگام تایپ:

```javascript
// در config.js
autoTranslate: true,
autoTranslateDelay: 500  // تاخیر به میلی‌ثانیه
```

### ذخیره تاریخچه

تاریخچه ترجمه‌ها به صورت خودکار در LocalStorage ذخیره می‌شود.

برای مشاهده تاریخچه در Console مرورگر:
```javascript
app.getHistory()
```

برای پاک کردن تاریخچه:
```javascript
app.clearHistory()
```

## ⚠️ حل مشکلات رایج

### خطای CORS
اگر با خطای CORS مواجه شدید:
- از Google Translate API یا Microsoft Translator استفاده کنید
- یا از یک سرور LibreTranslate محلی استفاده کنید

### ترجمه کار نمی‌کند
1. اتصال اینترنت خود را بررسی کنید
2. API Key را بررسی کنید (اگر از Google/Microsoft استفاده می‌کنید)
3. Console مرورگر را برای خطاها بررسی کنید (F12)

### زبان تشخیص داده نمی‌شود
- زبان تشخیص به صورت تقریبی است
- برای دقت بیشتر، می‌توانید زبان را به صورت دستی انتخاب کنید
- یا از API های تشخیص زبان استفاده کنید

## 📱 استفاده به عنوان Extension

برای تبدیل به Browser Extension:

1. یک پوشه `manifest.json` ایجاد کنید
2. فایل‌ها را در Extension قرار دهید
3. Extension را در مرورگر نصب کنید

## 🎯 نکات مهم

- برای استفاده در Production، حتماً از API Key خود استفاده کنید
- API Key را در Repository عمومی قرار ندهید
- برای استفاده بیشتر، محدودیت‌های API را در نظر بگیرید

---

**موفق باشید! 🎉**

