# 📱 راهنمای نصب روی موبایل

این راهنما به شما کمک می‌کند تا اپلیکیشن ترجمه شناور را روی موبایل خود نصب کنید.

## 🎯 روش‌های نصب

### روش 1: نصب به صورت PWA (Progressive Web App) - **پیشنهادی**

این روش ساده‌ترین و بهترین روش است. برنامه به صورت یک اپلیکیشن واقعی روی موبایل شما نصب می‌شود.

#### برای Android (Chrome):

1. **آپلود برنامه روی سرور**:
   - تمام فایل‌ها را روی یک سرور وب آپلود کنید (مثلاً GitHub Pages، Netlify، Vercel)
   - یا از یک سرور محلی استفاده کنید

2. **باز کردن در مرورگر Chrome**:
   - برنامه را در مرورگر Chrome موبایل باز کنید
   - آدرس باید با `https://` شروع شود (نه `http://`)

3. **نصب برنامه**:
   - منوی Chrome را باز کنید (سه نقطه در بالا)
   - گزینه **"Add to Home screen"** یا **"نصب برنامه"** را انتخاب کنید
   - یا پیام نصب را که به صورت خودکار نمایش داده می‌شود، تایید کنید

4. **استفاده**:
   - آیکون برنامه روی صفحه اصلی موبایل شما ظاهر می‌شود
   - می‌توانید مانند یک اپلیکیشن عادی از آن استفاده کنید

#### برای iOS (Safari):

1. **باز کردن در Safari**:
   - برنامه را در مرورگر Safari موبایل باز کنید
   - آدرس باید با `https://` شروع شود

2. **نصب برنامه**:
   - دکمه Share (مربع با فلش) را در پایین صفحه فشار دهید
   - گزینه **"Add to Home Screen"** یا **"افزودن به صفحه اصلی"** را انتخاب کنید
   - نام برنامه را تایید کنید و **"Add"** را بزنید

3. **استفاده**:
   - آیکون برنامه روی صفحه اصلی شما ظاهر می‌شود

---

### روش 2: استفاده از ابزارهای Build (تبدیل به APK/IPA)

اگر می‌خواهید برنامه را به صورت فایل APK (Android) یا IPA (iOS) بسازید:

#### گزینه A: استفاده از PWA Builder (ساده‌ترین)

1. به [PWABuilder.com](https://www.pwabuilder.com/) بروید
2. آدرس URL برنامه خود را وارد کنید
3. روی **"Build My PWA"** کلیک کنید
4. فایل APK یا Package را دانلود کنید
5. روی موبایل نصب کنید

#### گزینه B: استفاده از Capacitor

```bash
# نصب Capacitor
npm install -g @capacitor/cli

# ایجاد پروژه Capacitor
npx cap init

# اضافه کردن پلتفرم Android
npx cap add android

# اضافه کردن پلتفرم iOS
npx cap add ios

# کپی فایل‌ها
npx cap copy

# باز کردن در Android Studio / Xcode
npx cap open android
npx cap open ios
```

#### گزینه C: استفاده از Cordova

```bash
# نصب Cordova
npm install -g cordova

# ایجاد پروژه
cordova create myapp com.example.translation "Translation App"

# کپی فایل‌ها به www/
# سپس:
cordova platform add android
cordova platform add ios

# Build
cordova build android
cordova build ios
```

---

### روش 3: استفاده از سرور محلی (برای تست)

اگر می‌خواهید روی موبایل خود تست کنید بدون آپلود روی سرور:

#### استفاده از Python:

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

#### استفاده از Node.js:

```bash
# نصب http-server
npm install -g http-server

# اجرای سرور
http-server -p 8000
```

**نکته مهم**: برای PWA، باید از `https://` استفاده کنید. برای تست محلی:
- از `localhost` استفاده کنید
- یا از ngrok برای ایجاد HTTPS tunnel استفاده کنید

---

## 🖼️ ایجاد آیکون‌ها

برای نصب کامل PWA، نیاز به آیکون‌های مختلف دارید:

### روش 1: استفاده از ابزار آنلاین

1. به [PWA Asset Generator](https://github.com/onderceylan/pwa-asset-generator) بروید
2. یا از [RealFaviconGenerator](https://realfavicongenerator.net/) استفاده کنید

### روش 2: ایجاد دستی

یک آیکون 512x512 پیکسل ایجاد کنید و آن را در پوشه `icons/` قرار دهید.

سپس با استفاده از ابزارهای آنلاین یا نرم‌افزارهای ویرایش تصویر، سایزهای مختلف را ایجاد کنید:
- 72x72
- 96x96
- 128x128
- 144x144
- 152x152
- 192x192
- 384x384
- 512x512

### روش 3: استفاده از اسکریپت

می‌توانید از ابزارهای command-line استفاده کنید:

```bash
# با ImageMagick
convert icon.png -resize 72x72 icons/icon-72x72.png
convert icon.png -resize 96x96 icons/icon-96x96.png
# و غیره...
```

---

## 📤 آپلود روی سرور

### گزینه 1: GitHub Pages (رایگان)

1. یک Repository در GitHub ایجاد کنید
2. فایل‌ها را Push کنید
3. در Settings > Pages، Source را به `main` branch تنظیم کنید
4. آدرس برنامه شما: `https://username.github.io/repository-name/`

### گزینه 2: Netlify (رایگان)

1. به [Netlify.com](https://www.netlify.com/) بروید
2. فایل‌ها را Drag & Drop کنید
3. آدرس برنامه شما: `https://your-app-name.netlify.app`

### گزینه 3: Vercel (رایگان)

1. به [Vercel.com](https://vercel.com/) بروید
2. Repository را Connect کنید
3. Deploy کنید

### گزینه 4: Firebase Hosting

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

---

## ✅ بررسی نصب PWA

برای بررسی اینکه PWA به درستی کار می‌کند:

1. **بررسی Manifest**: 
   - در Chrome DevTools > Application > Manifest
   - باید تمام اطلاعات نمایش داده شود

2. **بررسی Service Worker**:
   - در Chrome DevTools > Application > Service Workers
   - باید Service Worker فعال باشد

3. **بررسی آیکون‌ها**:
   - در Chrome DevTools > Application > Manifest > Icons
   - باید تمام آیکون‌ها لود شوند

4. **تست نصب**:
   - در Chrome DevTools > Application > Manifest
   - روی "Add to homescreen" کلیک کنید

---

## 🔧 رفع مشکلات

### مشکل: پیام نصب نمایش داده نمی‌شود

**راه‌حل**:
- مطمئن شوید که از HTTPS استفاده می‌کنید
- Service Worker باید به درستی ثبت شده باشد
- Manifest.json باید معتبر باشد

### مشکل: آیکون‌ها نمایش داده نمی‌شوند

**راه‌حل**:
- مسیر آیکون‌ها را در manifest.json بررسی کنید
- مطمئن شوید که فایل‌های آیکون وجود دارند
- سایز آیکون‌ها باید صحیح باشد

### مشکل: برنامه Offline کار نمی‌کند

**راه‌حل**:
- Service Worker باید به درستی ثبت شده باشد
- فایل‌ها باید در Cache ذخیره شده باشند
- در Chrome DevTools > Application > Cache Storage بررسی کنید

---

## 📝 نکات مهم

1. **HTTPS ضروری است**: PWA فقط روی HTTPS کار می‌کند (یا localhost)
2. **Service Worker**: باید در root directory باشد
3. **Manifest**: باید در root directory باشد
4. **آیکون‌ها**: حداقل یک آیکون 192x192 و 512x512 نیاز است

---

## 🎉 پس از نصب

پس از نصب موفق:
- برنامه مانند یک اپلیکیشن عادی روی موبایل شما کار می‌کند
- می‌توانید آن را از صفحه اصلی باز کنید
- می‌توانید آن را Uninstall کنید مانند هر اپلیکیشن دیگر
- برنامه می‌تواند Offline کار کند (با Service Worker)

---

**موفق باشید! 🚀**

